.list_items
	each award in awards
		.list_item
			a.item_rm.award(href='#', id=award._id) -- удалить
			a.item_title(href='/admin/awards/edit/' + award._id, class=award.status =='hidden' ? 'hidden': '')= (award.i18n.title.get('ru') || '!!! ПУСТОЕ ПОЛЕ !!!')

.list_counter
	a.list_back(href='#'): b назад
	span= ' - '
	select.list_select
		- var i = 0;
		while i < count
			option(value=i selected= (i == skip / 10) )= i+1
			- i++
	span= ' - '
	a.list_next(href='#'): b вперед