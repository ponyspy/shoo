.list_items
	each event in events
		.list_item
			a.item_rm.event(href='#', id=event._id) -- удалить
			a.item_title(href='/admin/events/edit/' + event._id, class=event.status =='hidden' ? 'hidden': '')= (event.i18n.title.get('ru') || '!!! ПУСТОЕ ПОЛЕ !!!')

.list_counter
	a.list_back(href='#'): b назад
	span= ' - '
	select.list_select
		- var i = 0;
		while i < count
			option(value=i selected= (i == skip / 10) )= i+1
			- i++
	span= ' - '
	a.list_next(href='#'): b вперед