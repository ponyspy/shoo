.list_items
	each people in peoples
		.list_item
			a.item_rm.people(href='#', id=people._id) -- удалить
			a.item_title(href='/admin/peoples/edit/' + people._id, class=people.status =='hidden' ? 'hidden': '')= (people.i18n.name.get('ru') || '!!! ПУСТОЕ ПОЛЕ !!!')

.list_counter
	a.list_back(href='#'): b назад
	span= ' - '
	select.list_select
		- var i = 0;
		while i < count
			option(value=i selected= (i == skip / 10) )= i+1
			- i++
	span= ' - '
	a.list_next(href='#'): b вперед